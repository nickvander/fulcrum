# Missing Items

## Todo
- [ ] Verify if `rules_vitest` is available and works with Angular v21 in Bazel.
- [ ] Check for any breaking changes in Angular v21 that affect the application code.
- [ ] **CRITICAL**: Resolve `sass` compatibility issue in Bazel sandbox (likely needs `sass-embedded` or patched wrapper for `globalThis._cliPkgExports`).
